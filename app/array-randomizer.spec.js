import * as mocha from 'mocha';

mocha.describe("ArrayRandomizer", () => {
mocha.describe("constructor()", () => {
  });

  mocha.describe("randomizeArray()", () => {
    //TODO: the implementation of this is pretty arbitrary, leaving this out unless needed.
  });
});