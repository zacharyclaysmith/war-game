import * as mocha from 'mocha';

mocha.describe("Card", () => {
  mocha.describe("constructor()", () => {
    mocha.it("should throw an error if rank is not a positive number", () => {
      throw "Not Implemented"
    });

    mocha.it("should throw an error if suit is not a positive number", () => {
      throw "Not Implemented"
    });

    mocha.it("should set the rank property to the rank parameter", () => {
      throw "Not Implemented"
    });

    mocha.it("should set the suit property to the suit parameter", () => {
      throw "Not Implemented"
    });
  });
});